<%= form_with model: chat do |f| %>
		<%= f.text_area :message, style: "width:75%; height:31px;", autofocus: true, wrap:"physical" %>
		<%= f.hidden_field :room_id %>
		<%= f.submit "送信", class: "btn btn-sm", style: "color:white; background-color:#17a2b8; margin-bottom:22px;" %>
<% end %>